# LeadBridge — This root Dockerfile is deprecated.
# The project now uses separate Dockerfiles:
#   - server/Dockerfile   (TypeScript Fastify server)
#   - frontend/Dockerfile (Next.js frontend)
# See docker/docker-compose.yml for the full stack configuration.
