# Work Log — 2026-09-07

## Status: repo hygiene + backups + rebrand prep + 30-day trial fix — all pushed, CI green

## What was done

### ✅ Repo hygiene (commit `ee47c15` + `8aa051f`)
- Committed & pushed the 2026-09-04 fixes (SMS sender-ID, Sheets UI, auth emailSent, test DB rename) — CI green on first pass after two failing runs
- Fixed a CI landmine: workflow created `leadbridge_test`/`leadbridge_e2e` DBs but vitest expects `converza_*` → renamed in `ci-cd.yml`; local DB also renamed
- README rebranded (LeadBridge → Converza, admin email, /health examples)
- Deleted junk (`nul`, `dump.rdb`, 6 logs, 2 empty dirs); deprecated Railway docs with banners

### ✅ DB backups (commit `140dffd`) — awaiting Sarvesh's 3 secrets
- `scripts/backup-db.sh`: opt-in off-site upload to private Supabase Storage bucket `db-backups` (non-zero exit on failure)
- `.github/workflows/db-backup.yml`: scheduled backup (every 6h + manual)
- `docs/DB-BACKUP-SETUP.md`: Render Cron Job (recommended) + Actions setup, session-pooler warning, restore drill

### ✅ Rebrand prep (commit `140dffd`)
- CSP `connect-src` pinned to exact API origin (dropped `*.railway.app`/`*.vercel.app`/`*.onrender.com` wildcards)
- `docs/REBRAND-URL-SWITCH.md`: exact staged edits (render.yaml FRONTEND_URL, .env.local, Razorpay plan names, smoke tests) for after the Render/Vercel renames

### ✅ 30-day trial fix (this commit) — closes the code-vs-marketing gap
- **Found:** code gave 14-day trials (`auth.ts` signup + Google OAuth, checkout `trialDays: 14`) while ALL outreach scripts promise "free 30-day trial" — false advertising risk from lead #1
- **Fix:** single knob `TRIAL_DAYS` (default 30) in `config.ts`; both signup paths + STARTER checkout trial now read it; emails + register UI say 30 days; docs updated; `TRIAL_DAYS=30` added to render.yaml
- Cost exposure unchanged: `TRIAL_MAX_CALLS = 50` cap (~₹460/user) enforced in call.worker.ts regardless of trial length
- Business note surfaced by the change: STARTER = ₹18K for 100 calls = ₹180/call vs GROWTH ₹70/call — entry-tier cliff confirmed; annual pricing already exists in PLAN_DEFINITIONS

## Validation
- Server suite: **172 passed / 8 skipped** ✅ · server tsc ✅ · frontend tsc ✅

## What's next (Sarvesh-side)
1. Razorpay KYC (longest wait) + webhook registration
2. Supabase DB password rotation (leaked in git history, commit `c94c8d0`)
3. Render ENCRYPTION_KEY = local value
4. Create `db-backups` bucket + add backup secrets/cron (docs/DB-BACKUP-SETUP.md)
5. MessageBird number + IMAP creds + Render/Vercel renames (then REBRAND-URL-SWITCH.md)
6. Record 1 demo call + Render paid plan + Sentry DSN before broker #1
