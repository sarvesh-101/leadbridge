# Work Log — 2026-08-07

## Sync & Deployment Record (GitHub + Vercel)

### ✅ GitHub — fully synced
- **Commit `dc81ca2`** `feat: go-live hardening batch - billing/credits/limits/verification + work-log`
  - 90 files, 7,063 insertions, 853 deletions — all Round-2 hardening, billing fixes,
    subscription.service.ts, lead-limits.ts, credit system, new tests, work-log history
- **Commit `c4f2e64`** `chore: normalize .vercelignore for clean Vercel CLI deploys`
- Pushed to `origin/main` (github.com/sarvesh-101/leadbridge.git) — repo is up to date.
- Cleanup before commit: deleted 11 temp diagnostic scripts (`_*.cjs`, `_test_email.txt`,
  `tmp-cost-investigate.cjs`) so no junk entered the repo.
- **No secrets committed** — verified `git check-ignore` on `server/.env` + `frontend/.env.local`,
  and no secret-looking untracked files existed.

### ✅ Vercel — fresh production deploy LIVE
- **Production URL: https://leadbridge-seven.vercel.app** (aliased to fresh build, Ready in 2m)
- Root cause found: the Vercel project `leadbridge` had **NO env vars** → deployed site pointed
  at localhost. Fixed by setting production env vars in the dashboard via CLI:
  - `NEXT_PUBLIC_API_URL` → `https://casino-bunkbed-bronze.ngrok-free.dev/api/v1`
  - `NEXT_PUBLIC_WS_URL` → `wss://casino-bunkbed-bronze.ngrok-free.dev`
  - `NEXT_PUBLIC_APP_URL` → `https://leadbridge-seven.vercel.app`
  - `NEXT_PUBLIC_GOOGLE_CLIENT_ID` → unchanged (already set)
- **Deploy obstacle solved:** CLI tried uploading 1.4 GB (node_modules + .next included despite
  `.vercelignore`) → hit Vercel's 100 MB single-file limit. Worked around by deploying from a
  clean staging folder (`src`, `public`, configs only = 1.9 MB) with Root Directory = `frontend`
  matching project settings. Vercel ran `npm ci && next build` on their servers.
- **Live verification after deploy:**
  - `/auth/verify-email` → **HTTP 200** (was 404 before — new page now live)
  - `/auth/register` → 200
  - `/` landing → 200
  - 0 `localhost` references in served HTML (P0-1 fixed on the deployed site)
  - Backend via ngrok → HTTP 200 (reachable)

### ⚠️ Known items for next session
1. **Redeploy process** — this deploy was CLI/staging-based, NOT triggered by the git push.
   Unless Vercel Git integration + Root Directory (`frontend`) are confirmed, future pushes to
   `main` will NOT auto-redeploy. Redeploy procedure: staging folder flow (documented above) or
   enable Git integration in Vercel dashboard.
2. **Env vars also added to wrong project `leadbridge-78b`** during investigation — harmless
   (ngrok URLs on a test project); optional cleanup.
3. **Production blocker unchanged:** live frontend depends on the local backend via ngrok tunnel.
   If the tunnel is down, the site's API calls fail. Real backend hosting (Railway/VPS) still pending.
4. **WhatsApp** — real number still "In review" at Meta; test number only.
5. **MessageBird / Supabase / Sentry keys** still empty in server/.env.
