# Work Log — 2026-08-15

## Status: Phase 2.2 margins analysed + 1.5 email leftover fixed

### ✅ Phase 2.2 — Per-plan margin analysis (code-free, decision doc written)
- **NEW `docs/plan-margins.md`** — full sensitivity table for all three plans at 2/4/6-min
  average call durations.
- **Findings:**
  - STARTER (₹18K/100 calls): 84–94% margin — safe at every realistic duration.
  - GROWTH (₹35K/500 calls): 60–86% margin — safe.
  - PRO (₹60K/5,000 cap): **23% at 2-min avg calls, NEGATIVE (−₹32K/mo) at 4-min avg**.
    Break-even cap at 4-min avg is ~3,250 calls — the current 5,000 cap is above it.
  - Offline/prepaid calls (₹70/call): 61–87% margin — safe.
- **⚠️ Weak assumption:** `AVG_CALL_DURATION_MINUTES=2` is optimistic for real-estate
  telecalling (sales calls run 3–5+ min). Plan as if 4 min until Phase 4.2 real data arrives.
- **Recommendation (in doc):** lower `PRO_MONTHLY_CALL_CAP` to ~2,000 (worst case still ~8%
  margin; one env var change), re-tune after real call-duration data. A vs B vs C options
  documented — decision left to Sarvesh.

### ✅ Phase 1.5 leftover — last dead-domain email swapped (code)
- `frontend/src/app/dashboard/forwarding/page.tsx`: `forward@leadbridge.com` display default +
  env-var example → `forward@converza.tech` (2 spots). Grep confirms 0 remaining
  customer-facing `@leadbridge.com` refs anywhere in the repo.

## Validation
- Frontend `tsc --noEmit` ✅ | Frontend lint ✅ (see session commands)

## What changed (files)
- `docs/plan-margins.md` (new)
- `frontend/src/app/dashboard/forwarding/page.tsx`
- `work-log/launch-plan.md` (2.2 ✅, 1.5 leftover ✅)

### ✅ PRO cap lowered to 2,000 (decision applied 2026-08-15)
- `server/.env`: `PRO_MONTHLY_CALL_CAP=5000` → `2000` (verified via grep).
- `server/src/config.ts`: code default lowered 5000 → 2000 + comment explaining the margin
  analysis (so the safe value holds even without the env var).
- `docs/plan-margins.md` + `RAILWAY-DEPLOY.md` env table updated to match.
- Worst case (6-min avg calls at 2,000) = ~₹55,400 cost vs ₹60K revenue → still ~8% margin.

### 🎨 Frontend redesign — professional palette (2026-08-15, while Sarvesh AFK)
- Complaint: landing page + frontend looked "AI-generated" — childish violet/mint palette + loud gradients.
- **Global hex swap across `frontend/src` (93 files, landing + dashboard together):**
  - Accent `#4F6EF7` (indigo-violet) → `#3B82F6` (professional blue)
  - Success `#22D3A5` (mint-teal) → `#10B981` (emerald)
  - Purple-gray text `#6B6B8A` → neutral slate `#8B93A3`; headings `#F0F0F8` → `#F2F4F8`
  - Charcoals `#111118`/`#1A1A24`/`#2A2A3A`/`#3A3A52`/`#0A0A0F` → neutral slate tones
- **Design tokens** (`globals.css` CSS vars + `tailwind.config.ts` primary/brand/surface/ink/text) updated to match; blue-50..900 scale.
- **Gradient cleanup:** FeatureShowcase 12-feature rainbow icon gradients → uniform blue chip; PricingSection spinning conic-gradient ring → static inset ring; ROI bar fills flattened to solid.
- GlobeCanvas particles/wireframe violet `0x4f6ef7` → blue `0x3b82f6`.
- Kept gold `#C9A84C` (premium accent) + rose `#F43F5E` (danger) — intentional.
- Validation: frontend `tsc --noEmit` ✅ · eslint ✅ · `next build` ✅ (all routes compile).

### ✅ Phase 1.4 + 2.3 — docs drafted (2026-08-15, AFK session)
- **`sales/whatsapp-business-verification.md`** — Meta verification checklist: entity name
  rule (Meta = GST/CoI/MSME = website = bank), BM setup, submission steps, rejection fixes.
  Action itself needs Sarvesh (entity name + docs) once 1.1 decides the entity.
- **`sales/payment-loop-test.md`** — 8-step production payment-loop runbook (trial → GROWTH
  checkout → real charge → webhook asserts → renewal sim → cancel/refund → failure paths →
  teardown). Ready to run the moment Railway deploys.

### ✅ Live-site QA of new palette (2026-08-15)
- Started `next dev`, hit `/` + `/auth/register` + `/auth/login` + `/legal/privacy` +
  `/dashboard/settings` — all HTTP 200, zero compile/runtime errors in dev log.
- Rendered HTML: new palette confirmed (167× #3B82F6, 186× #8B93A3, 128× #F2F4F8, etc.),
  0 old violet colors.
- **Contrast audit** (WCAG): text/headings/cards all PASS AA. One failure found: white text on
  emerald buttons (#10B981 = 2.54:1). **Fixed** — buttons that had white-on-emerald now use
  deep emerald `#047857` (5.48:1 AA) in chat send, sheets-sync, setup wizard (2×), step
  indicator, admin wizard shell; leftover `bg-gradient` button fills flattened to solid.
- Re-verified: `tsc --noEmit` ✅ · `next build` ✅ (63/63 pages).

## What's next
1. Sarvesh-side (unchanged): Railway deploy (0.1), MessageBird key (0.4), WhatsApp app
   registration on +91 72088 55916 (0.3).
2. Re-tune `PRO_MONTHLY_CALL_CAP` after Phase 4.2 real call-duration data.
3. Visual QA pass on the live site (new palette) once the dev server is up.
