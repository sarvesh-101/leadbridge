# Work Log — 2026-08-10

## Status: CODE TASKS DONE + VALIDATED (145 tests) · DEPLOY/KEYS NEED SARVESH

## What was done

### ✅ Phase 2.1 — Landing page claims made honest (code, live-verified via typecheck/lint)
- **`ComparisonSection.tsx`** — \"500+ leads/month\" and \"100% consistent\" now carry a `*`
  footnote: \"Platform capability, not a guarantee. Actual results vary by broker, market and lead flow.\"
- **`ROISection.tsx`** — added an explicit \"Illustrative calculator — assumptions\" box
  (400 leads/month, 2 × ₹25K telecallers, 12 vs 36 bookings) + \"More bookings (example)\" /
  \"in this scenario\" labels. The section was already captioned ROI CALCULATOR.

### ✅ Phase 1.3 — DPDP Act 2023 (privacy consent + erasure) — code done
- **Schema + migration `20260810000000_dpdp_consent`**: `Client` gets `consentGivenAt`,
  `consentVersion` (default \"1.0\"), `dataErasureRequestedAt`, `dataErasureProcessedAt`.
- **Register requires consent** (`POST /auth/register`): 400 + `consentRequired` without it;
  stores consent timestamp + version. Google OAuth signup records consent too.
  Centralized `PRIVACY_POLICY_VERSION` in `config.ts`.
- **Privacy endpoints** (`client/settings.ts`): `GET /me/privacy`, `POST /me/privacy/consent`,
  `POST /me/privacy/erasure-request` — atomic via conditional `updateMany` (double-submit safe,
  no duplicate admin notification), idempotent, 30-day SLA message.
- **Admin** (`admin/clients.ts`): `?erasureRequested=true` list filter + `dataErasureProcessedAt`
  whitelisted so the erasure queue is one click away.
- **Frontend register** — required consent checkbox (DPDP wording, links ToS + Privacy Policy).
- **Frontend Settings → Privacy & Data tab** — consent status chip, Privacy Policy link,
  \"Request data erasure\" with confirm modal (merge-response fix so consent chip never flickers).
- **Privacy Policy page** — updated (Aug 10, 2026) + DPDP rights/erasure instructions added.

### 🟦 Phase 0.1 prep — Railway deploy guide written (deploy itself needs Sarvesh)
- **NEW `RAILWAY-DEPLOY.md`** — full step-by-step: Railway project, Postgres + Redis plugins,
  server service env table (incl. `DATABASE_URL_PRISMA` requirement), 7 worker services with
  start commands, migrations via laptop (`npx prisma migrate deploy`), Vercel env + CSP switchover,
  post-deploy checklist, cost estimate.
- Verified no code blockers: CORS = `config.FRONTEND_URL` (already the Vercel domain), DEMO_MODE
  production guard present, Docker image CI-built.

### ✅ Phase 0.5 — Supabase storage DONE + live-verified (2026-08-10)
- `SUPABASE_URL` (https://oavzflfdjluxvdlymbug.supabase.co) + `SUPABASE_SERVICE_KEY` set in `server/.env`.
- Bucket `call-recordings` made **public** (via `PUT /storage/v1/bucket/call-recordings` — PATCH 404s on
  this project; PUT is the route).
- **Live test passed**: upload → public URL → read HTTP 200 → signed URL → delete.
- **Code fix**: `storage.service.ts` `deleteFile` switched from the deprecated
  `DELETE /object/{bucket}/{path}` (400 on new Supabase projects) to the current bulk API
  `DELETE /object/{bucket}` with `{ prefixes: [path] }` body (verified 200 live).

### ✅ Phase 1.5 — Legal contact emails DONE (2026-08-10)
- Real mailbox purchased: **`support@converza.tech`** → swapped ALL customer-facing
  dead `@leadbridge.com` links: landing footer Contact, FAQ CTA, ToS §11, Privacy Policy §10.
- Outbound `FROM_EMAIL` in `server/.env` verified as the real Gmail SMTP user (sends from live inbox).
- `sales/support-channel.md` email row now ✅; launch-plan 1.5 ticked.
- Validation: frontend tsc ✅, lint ✅, grep confirms 0 remaining customer-facing `@leadbridge.com` refs.
- ⚠️ Note: `forward@leadbridge.com` display default remains on Lead Forwarding page (env/webhook overrides it).

### ✅ Phase 0.3 — WhatsApp switched to REAL number + LIVE VERIFIED (2026-08-12)
- Discovered the old token had **expired Aug 4** (WhatsApp sends were silently failing for a week).
- Got a **permanent SYSTEM_USER token** (expires: never) — verified via `debug_token`.
- Real number **+91 72088 55916** added to Meta, code-verified, display name `LeadBridge`.
- `.env`: `WHATSAPP_TOKEN` (permanent) + `WHATSAPP_PHONE_ID` → `1226070940590994` (was test number `1174238042447407`).
- **LIVE E2E TEST PASSED**: real message sent via `sendTextMessage` from +91 72088 55916 to +91 7045525531 (`wamid.HBgM...`).
- ⚠️ Open: WABA ID in `.env` is still the old test account (cosmetic, admin panel only); webhook URL needs deploy (Phase 0.1).

### ⏳ PENDING (next session — WhatsApp app registration)
- **Task: create the WhatsApp app account on +91 72088 55916** (the new business number).
  WhatsApp app showed "temporarily unavailable" cooldown while registering the SIM.
- Why: end-to-end delivery test needs a real WhatsApp account on the receiving number.
  Send-side is ALREADY proven (Meta accepts every send; returns wamid).
- Rules: don't spam retries (extends cooldown); try SMS code, else "call me" option;
  wait 1h+ between attempts; may take 24–72h if number had previous WhatsApp baggage.
- After account is created: msg +91 72088 55916 ("hi") to open the 24h window → resend
  the end-to-end test → confirm delivery → then optionally fix WABA ID in .env.

### 🔗 Keys still waiting (0.4 / 2.10) — setup links given
- MessageBird SMS (`MESSAGEBIRD_API_KEY`), Sentry (`SENTRY_DSN`). No code changes needed — config.ts already reads them.

### ✅ Phase 3 — Sales & onboarding readiness (docs drafted, 2026-08-10)
- **`sales/onboarding-guide.md`** — 1-page Hinglish guide: territory → AI agent → number attach →
  test call → 4 lead routes (SMS/email/sheets/CSV) → knowledge base → pipeline explainer → plans.
- **`sales/territory-exclusivity-agreement.md`** — draft contract written as a **business commitment**
  (accurate to the soft territory model — LeadBridge won't onboard a second broker in a claimed area;
  not a hard technical block). Needs entity name + legal review before first signature.
- **`sales/demo-script.md`** — 90-second live demo flow + objection handling (price, trust in AI,
  leads source, cancel policy) + pre-demo checklist.
- **`sales/support-channel.md`** — WhatsApp Business (+91 7045525531) ready now; response-time
  promises (60 min urgent / 4 business hours normal); daily 15-min routine; escalation path at 5/20 brokers.

### ✅ Phase 0.6 — Backups + uptime monitoring (tooling ready, scheduling after 0.1 deploy)
- **`scripts/backup-db.sh`** — pg_dump → gzip, size sanity-check, retention (default 10), optional S3 off-site.
- **`scripts/uptime-check.sh`** — hits `/health`, alerts only on state transition (no spam), works with
  Slack/Discord/Telegram webhooks; health endpoint already checks DB + Redis + queues.
- **`infrastructure/monitoring/railway-monitoring.md`** — Railway setup guide: built-in Postgres backups +
  cron-job dumps + free uptime service or cron checker + launch-day kill-check/restore drill + weekly ops hook.
- Both scripts pass `bash -n` syntax check.

## What changed (files)
- `server/prisma/schema.prisma`, `server/prisma/migrations/20260810000000_dpdp_consent/migration.sql`
- `server/src/config.ts` (PRIVACY_POLICY_VERSION), `server/src/routes/auth.ts` (consent),
  `server/src/routes/client/settings.ts` (privacy endpoints), `server/src/routes/admin/clients.ts`
- `server/src/__tests__/privacy.test.ts` (new, 5 tests), `auth-flow.test.ts` (consent cases)
- `frontend/src/app/auth/register/page.tsx`, `frontend/src/app/dashboard/settings/page.tsx`,
  `frontend/src/app/legal/privacy/page.tsx`
- `frontend/src/components/marketing/ComparisonSection.tsx`, `ROISection.tsx`
- `RAILWAY-DEPLOY.md` (new), `work-log/launch-plan.md` (2.1 ✅, 1.3 ✅, 0.1/0.4/0.5 notes)

## Validation
- Server `tsc --noEmit` ✅ | Frontend `tsc --noEmit` ✅ | Server lint 0 errors ✅ | Frontend lint ✅
- `vitest run` → **145 passed / 8 skipped / 0 failed** (was 139 — +6 new tests)
- Code-reviewed (deepseek) × 1 round — reviewer flags all closed (E2E-safe check, atomic erasure,
  state-merge fix, version centralization).

## What's next (needs Sarvesh)
1. **Run `RAILWAY-DEPLOY.md`** (Phase 0.1) — create Railway account, add Postgres+Redis,
   deploy server + 7 workers, run migrations, then flip Vercel env + CSP. THE big one.
2. **Paste keys** from the setup links: MessageBird (0.4), Supabase (0.5), Sentry (2.10).
3. After deploy: live-verify register → verify email → login against the Railway backend.
4. Optional: WhatsApp real-number swap (0.3) when Meta approves +91 72088 55916.
